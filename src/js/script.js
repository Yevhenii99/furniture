window.addEventListener('DOMContentLoaded', () => {

    function openModal() {
        const modal = document.querySelector('.promo_btn');
        const box = document.querySelector('.promo');


        modal.addEventListener('click', () => {
            // const form = document.createElement('div.form');
            // form.style.cssText = `
            // position: absolute;
            // z-index: 2;
            // width: 250px;
            // height: 250px;
            // border: 2px solid black;
            // background: yellow;
            // `;
            // form.prepend(box);

        });
    }
    openModal();
});